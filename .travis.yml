language: python
python:
    - 2.7
    - 3.4
    - 3.5
    - 3.6
before_install:
    - sudo apt-get -y update
install:
    - pip --no-cache-dir install codecov --quiet 
    - pip --no-cache-dir install -r requirements.txt --quiet 
script:
    - nosetests
after_success:
    - codecov